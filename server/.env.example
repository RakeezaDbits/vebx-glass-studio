# Database (MySQL)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=vebx_studio

# JWT for admin
JWT_SECRET=your-secret-key-change-in-production

# Gmail SMTP (for form emails to support@vebx.run, etc.)
SMTP_USER=your-gmail@gmail.com
SMTP_APP_PASSWORD=your-16-char-app-password

PORT=3001
